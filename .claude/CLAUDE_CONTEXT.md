# Project Context

The canonical project context lives at the repo root:

**`H:\dNFTpet\BROskiPets-LLM-dNFT\CLAUDE.md`** — read that first.

Also at root: `HANDOFF.md` (current session state), `AGENT_SYNC_NOTES.md` (cross-repo contract).

This `.claude/` folder holds local Claude config (skills, settings) only.
